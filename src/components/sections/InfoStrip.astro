---
import { facility } from '../../data/facility';
---

<div class="info-strip reveal">
  <span>
    <span data-lang-ja>営業時間 {facility.hours.open} — {facility.hours.close}</span>
    <span data-lang-en>Hours {facility.hours.open} – {facility.hours.close}</span>
  </span>
  <span class="sep">/</span>
  <span>
    <span data-lang-ja>火曜定休</span>
    <span data-lang-en>Closed Tuesdays</span>
  </span>
  <span class="sep">/</span>
  <span>
    <span data-lang-ja>駐車場無料 100台</span>
    <span data-lang-en>Free Parking · 100 Spaces</span>
  </span>
  <span class="sep">/</span>
  <span>
    <span data-lang-ja>{facility.payment}</span>
    <span data-lang-en>{facility.paymentEn}</span>
  </span>
</div>

<style is:global>
  .info-strip {
    display: flex;
    justify-content: center;
    gap: 16px;
    padding: 40px 24px;
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.2em;
    color: var(--ink-dim);
    text-transform: uppercase;
    flex-wrap: wrap;
    background: #f4f5f1;
  }
  .info-strip .sep {
    color: var(--ink-faint);
  }

  @media (max-width: 640px) {
    .info-strip {
      flex-direction: column;
      gap: 8px;
      align-items: center;
    }
    .info-strip .sep {
      display: none;
    }
  }
</style>
